---
name: Image Vision
version: 1.0.0
category: tool
description: Image analysis with context-aware prompting and color extraction
triggers:
  keywords: [image, vision, analyze, describe, photo, picture]
  intent: 'User wants analysis or description of an image'
---

**DoD:** Read prompt first -> identify context -> never guess identity -> up to 8 hex swatches -> answer the prompt
